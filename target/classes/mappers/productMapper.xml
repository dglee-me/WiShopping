<?xml version="1.0" encoding="UTF-8"?>

<!-- DTD 선언 -->
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="productMapper">
	<!-- Category Register -->
	<insert id="register">
		INSERT INTO PRODUCT(PNO,CATEGORY1,CATEGORY2,PNAME,BRAND,INVENTORY,PRICE,PRODUCT_DESC,PRODUCT_URL,PRODUCT_THUMURL,SHIPPING,SHIPPINGDAY)
		VALUES(pno_seq.nextval,#{category1},#{category2},#{pname},#{brand},#{inventory},#{price},#{product_desc},#{product_url},#{product_thumurl},#{shipping},#{shippingday})
	</insert>
	
	<!-- Cateogry list -->
	<select id="list" resultType="com.lee.myapp.domain.ProductVO">
		SELECT *
		  FROM PRODUCT
		 WHERE CATEGORY1=#{category} 
		 ORDER BY PNO DESC
	</select>
	
</mapper>